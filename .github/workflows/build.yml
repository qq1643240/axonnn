name: Build Tweak

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Theos
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential git curl wget perl clang lld llvm \
            libxml2-dev libssl-dev zlib1g-dev dpkg-dev unzip xz-utils \
            libbz2-dev liblzma-dev fakeroot rsync sed grep lzma-dev
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV
          
      - name: Install iOS SDK
        run: |
          set -e
          cd $THEOS/sdks
          echo "Current SDKs:"
          ls -la
          echo "Downloading SDKs..."
          curl -L --retry 5 --retry-delay 10 --fail -o sdks.zip https://github.com/theos/sdks/archive/refs/heads/master.zip
          echo "Download complete, extracting..."
          unzip -q sdks.zip
          mv sdks-master/*.sdk .
          rm -rf sdks.zip sdks-master
          echo "Final SDKs:"
          ls -la
          
      - name: Build Package
        run: |
          make clean
          make package FINALPACKAGE=1
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Axon-deb
          path: ./packages/*.deb